# 项目配置文件
# 对该文件做出修改后，请点击 RStudio 菜单栏的 Session -> Restart R 重启 Session
# 改变才能生效

# 启动当前项目的虚拟环境
source("renv/activate.R")

# 配置包安装源为南方科技大学（深圳）
options(repos = c(CRAN = "https://mirrors.sustech.edu.cn/CRAN/"))

# 配置 print 函数最大输出行数（默认为 10 行）
options(max.print = 10000)
